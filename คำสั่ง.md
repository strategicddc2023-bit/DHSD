git add .
git commit -m "update project"
git push origin main

ก่อนเริ่มเขียนหรือแก้ไขโค้ดใด ๆ ให้อ่านไฟล์แผนผังโครงสร้างระบบจาก:
1. `graphify-out/GRAPH_REPORT.md`
2. `graphify-out/graph.json` (หรือใช้คำสั่ง query ของ Graphify)

เพื่อทำความเข้าใจ Module, Dependency และความเชื่อมโยงของไฟล์ในระบบ SmartDSP ทั้งหมดก่อนเสมอ
เมื่อฉันสั่งงาน:
- ให้ค้นหาไฟล์และฟังก์ชันที่เกี่ยวข้องจาก Graphify ก่อน
- ห้ามไล่ค้นหาหรือสแกนไฟล์ทั้งระบบโดยไม่จำเป็นเพื่อประหยัด Token
- เมื่อพบตำแหน่งที่ถูกต้องแล้ว ค่อยเปิดอ่านเฉพาะไฟล์ที่จำเป็นต้องแก้ไขเท่านั้น
-ติดตั้ง Auto-update อัตโนมัติทุกครั้งที่ git commit (Git Hook) 🪝
ไม่ต้องคอยพิมพ์สั่งเอง รันคำสั่งนี้ครั้งเดียว ทุกครั้งที่มีการ Commit โค้ด ระบบจะอัปเดต Graph ให้ทันที: & "D:\graphify-8\.venv\Scripts\graphify.exe" hook install

ตอบกลับสั้นๆ ไม่ต้องอธิบาย
เข้าใจคำสั่งหรือไม่ยังไม่ต้องเขียน code